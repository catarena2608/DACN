# Deploy

Deployment assets owned by `dacn-app`.

```text
compose/   Local Docker Compose stack
helm/      Application Helm chart
nginx/     Local reverse proxy image/config
```

FluxCD desired state should live in a future `dacn-gitops` repository.
