name: Security

on:
  workflow_call:

jobs:
  gitleaks:
    name: Gitleaks — secret scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   

      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: results.sarif
        continue-on-error: true

  npm-audit:
    name: npm audit — full monorepo
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Audit all services
        run: |
          SERVICES=("services/backend-auth" "services/backend-product" "services/backend-order" "apps/gateway" "apps/frontend")
          FAIL=false

          echo "| service | critical | high | moderate | low | result |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|----------|------|----------|-----|--------|" >> $GITHUB_STEP_SUMMARY

          for SVC in "${SERVICES[@]}"; do
            [ ! -f "$SVC/package.json" ] && continue
            cd "$SVC"
            npm ci --silent 2>/dev/null || true
            npm audit --json --omit=dev > /tmp/audit.json 2>&1 || true

            read C H M L <<< $(node -e "
              try {
                const v = require('/tmp/audit.json').metadata?.vulnerabilities || {};
                process.stdout.write([v.critical||0,v.high||0,v.moderate||0,v.low||0].join(' '));
              } catch(e) { process.stdout.write('0 0 0 0'); }
            ")

            if [ "$C" -gt 0 ] || [ "$H" -gt 0 ]; then
              echo "| \`$SVC\` | $C | $H | $M | $L | FAIL |" >> $GITHUB_STEP_SUMMARY
              FAIL=true
            else
              echo "| \`$SVC\` | $C | $H | $M | $L | OK |" >> $GITHUB_STEP_SUMMARY
            fi
            cd - > /dev/null
          done

          if [ "$FAIL" = "true" ]; then
            echo "::warning::npm audit found HIGH/CRITICAL vulnerabilities. Non-blocking in lab mode."
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "> npm audit found HIGH/CRITICAL vulnerabilities. This is reported only and does not block lab CI." >> $GITHUB_STEP_SUMMARY
          else
            echo "No HIGH/CRITICAL vulnerabilities"
          fi
  sonarqube:
    name: SonarQube Analysis (optional)
    runs-on: ubuntu-latest
    env:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
    steps:
      - name: Skip SonarQube when secrets are not configured
        if: ${{ env.SONAR_TOKEN == '' || env.SONAR_HOST_URL == '' }}
        run: |
          echo "::notice::Skipping SonarQube because SONAR_TOKEN or SONAR_HOST_URL is not configured."
          echo "SonarQube skipped: SONAR_TOKEN or SONAR_HOST_URL is not configured for this lab repository." >> $GITHUB_STEP_SUMMARY

      - name: Checkout code
        if: ${{ env.SONAR_TOKEN != '' && env.SONAR_HOST_URL != '' }}
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: SonarQube Scan
        if: ${{ env.SONAR_TOKEN != '' && env.SONAR_HOST_URL != '' }}
        uses: sonarsource/sonarqube-scan-action@master
        continue-on-error: true
        with:
          args: >
            -Dsonar.projectKey=E-commerceApplication
            -Dsonar.sources=.
            -Dsonar.exclusions=**/node_modules/**,**/test/**
