# Packages

Internal shared packages will live here when duplication becomes meaningful.

Examples:

```text
shared-config/
logger/
api-contracts/
```

This folder is intentionally empty for now because the current services are still small.
