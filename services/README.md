# Services

Business services live here. Each service owns its runtime, package file, Dockerfile, routes, controllers, services, models, and utilities.

```text
auth/      Authentication and token flow
product/   Product catalog
```
